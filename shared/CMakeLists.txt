# Shared components
# This file can be used for future shared code between projects

# For now, widgets remain in their respective projects due to different interfaces
# (wg:: namespace for editor, rn:: namespace for runtime)
